<li>
    {{burger_name}}

    <button class="change-devoured" data-id="{{id}}" data-newdevour="{{devoured}}">
        {{#if devoured}}DEVOUR!{{else}}RE-ORDER!{{/if}}
    </button>

	<button class="delete-burger btn" data-id="{{this.id}}" style="background-color: #C66433; color: white;">Delete!</button>
</li>